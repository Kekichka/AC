import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Порожня конфігурація за замовчуванням — навмисно.
  // Студенти додають сюди налаштування в міру потреби.
};

export default nextConfig;
